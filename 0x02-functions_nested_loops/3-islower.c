#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks for lower case characters
 *
 * Return: Always 1 (Success), 0 (Otherwise)
 */

int _islower(int c)
{
	if(islower(c))
		return (0);
	else
		return (0);

}

